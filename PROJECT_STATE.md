# PROJECT_STATE — ghost-code (folder still `pacman-code-trainer` locally)
_Last compiled: 2026-09-02 — Portfolio Cook Out Prompt 4 (public-truth sync)_

## Current Status
**LIVE at the custom URL.** Ghost Code serves at **https://ghostcode.skypistudio.com** (custom domain, HTTPS/HSTS), confirmed reachable and matching current `main` at time of this sync. `main = origin/main = 1e6b963` (2026-07-31) — the "terminal, not arcade" identity, the light-mode "Platinum" theme, and the identity/attribution pass (start-screen credit, HUD relabel) are all live. Portfolio-side claims about Ghost Code were not re-verified here — this repo's own public surfaces (README, docs, metadata) were the scope; see `qa-reports/2026-09-02_GhostCode_CookoutP4_PublicTruthSync.md` for the full receipt.

## Branch topology
- **`main` `1e6b963`** = `origin/main` — canonical + LIVE. Includes everything through the light-mode Platinum theme and the title-screen theme-toggle reachability fix.
- Old pre-rebrand stale branches (still unpruned as of this pass, none blocking): `Taylor/a11y/category-aria-labels-2026-05-27`, `a11y/auto-2026-05-25`, `community/auto-2026-05-25-casey-readme`, `cycle/auto-2026-05-23-ui`, `dani/ghost-code-concept-2026-06-03`, `feat/auto-2026-05-25-shamus-git-cards`, `feat/auto-2026-05-25-shamus-git-ui`, `release/auto-2026-05-25`, `test/pacman-cards-data-validator-2026-05-25`, `test/pacman-empty-deck-robustness-2026-05-25`.
- Newer local-only staging branches from the 2026-06→2026-07 work, already landed on `main` (candidates for a future prune sweep, not actioned here — out of Prompt 4 scope): `docs/truth-reconcile-2026-06-19`, `feat/ghostcode-polish`, `feat/open-to-work-footer-2026-06-19`, `theme/light-mode`, `fix/theme-toggle-reachable`, `uplift/p1-floor` … `uplift/p6-closeout`.
- This worktree's branch (`claude/portfolio-cookout-prompt-bbed3f`) = current `origin/main`, clean — created for the Portfolio Cook Out Prompt 4 public-truth sync; no push/merge/deploy performed.

## Context Snapshot
**Ghost Code** — a calm, modern terminal-command trainer for memorizing Claude Code + macOS terminal + Git commands (rebranded away from an earlier Pac-Man-styled build on 2026-06-04, over trademark/trade-dress risk — see `DECISIONS_LOG.md`). The Phantom mascot, command-token answers, danger-flash (no creature enemies), spirit-pip lives, dark theme by default with a light "Platinum" theme (system/light/dark, `T` shortcut + title-screen/Settings controls). Single `index.html` (~3.3k lines, inline CSS+JS) + `cards.js` (**56 cards**: 20 Claude Code, 20 macOS terminal, 16 Git). Zero deps, no build, no framework, no TS, **no DB / network / auth / PII**. Green gate = `node --check cards.js` + extracted inline `<script>` + the theme-boot `<script>` + `node test/cards.test.js` + the CI theme-contract grep. Data = `localStorage['gc.v1']` (one-time migration from legacy `pmct.v1`); `hi` there is the numeric high score, not a player name. GitHub repo: `Skypie99/ghost-code`. Live: `https://ghostcode.skypistudio.com`.

## What shipped (full arc, ledger-style)
- **2026-06-03 evening — Full Pac-Man release shipped:** P0–P7 live (`af4a3ca`), then mobile flex-wrap fix (`24e5fe1`).
- **2026-06-03 night — Legal de-risk:** Jordan flagged Pac-Man as HIGH trademark/trade-dress risk; portfolio card pulled.
- **2026-06-04 — Ghost Code rebrand:** Dani's original "The Phantom" identity (`1912794`) + AA-contrast polish (`dc3c022`); repo renamed `pacman-code-trainer → ghost-code` on GitHub; portfolio re-added under new identity.
- **2026-06-04 — Custom domain wired:** CNAME file (`7ab62c6`); Pages cname=ghostcode.skypistudio.com + https_enforced=true; portfolio `links` restored & repointed (skypistudio.com main `fcb9103`). _(main later advanced to `2743a9d` with HUD-bracket + favicon + OG polish.)_
- **2026-06-05 — Mobile-native + WCAG 2.2 AA + polish (Sky-direct):** branch `feat/ghost-mobile-a11y-2026-06-05`, 6 per-area commits, `index.html` only. Touch tuning (viewport-fit, touch-action, safe-area), fully responsive portrait layout (compass diamond scales/centres, no clipping; mode-toggle↔HUD overlap fixed), **swipe-to-answer** (up/right/down/left → N/E/S/W), on-screen **50/50 button** (was keyboard-only), WCAG 2.2 AA (✓/✗ not-colour-only, flash capped <3/s, targets ≥24px, pause announced). 56-card green gate green throughout; desktop unchanged. Report: `qa-reports/2026-06-05_GhostCode_Mobile_A11y_Polish.md`. Reality clarified: it's a 4-token directional quiz, not a maze. Open follow-up: learn mode hides the question behind the hint panel (pre-existing on desktop too).
- **2026-06-18/19 — Identity/attribution pass + polish sweep:** start-screen "Built by Sky Halisky" + Source credit, HI-SCORE→BEST HUD relabel, `@supports` wordmark fallback (merged at `6846b3d`); OG card regenerated with truthful alt text, favicon set recolored, category naming unified, fonts preloaded (kill FOUT), title→board transition sequenced, pause overlay converted to a real focus-trapped modal, "open to thoughtful product collaborations" link added. A same-window partial truth-reconcile pass (`463a816`, 2026-06-19) updated this file's Current Status/Branch sections but missed the Context Snapshot above — that's the gap this 2026-09-02 pass closes.
- **2026-07-17→29 — `uplift/p1`…`p6` trains:** layout de-collision at 600/768/1440 breakpoints, HUD label legibility, the win-beat animation state, per-category "this-run" mastery credit alongside lifetime mastery, the DRILL MISSED review flow, the learn-progress-bar made properly `inert` behind title/pause/game-over, and mastery-bar edge cases (100% clamp, active-deck filtering).
- **2026-07-31 — Light-mode "Platinum" theme:** tokenized the palette (dark values byte-preserved), measured 3 candidate light palettes, built the mechanism + edge cases (`0cfbb68`…`76fdfc3`); parked a known cosmetic bug (Phantom renders one-eyed on some phones, `38f98bc` — not fixed, tracked as a non-goal for now); then fixed the title-screen `THEME` button being unreachable before a game started (`1e6b963`).
- **2026-09-02 — Portfolio Cook Out Prompt 4 (this pass):** public-truth sync only — no product/UI changes. Verified the deck (56/56), modes, controls, theme system, and metadata against source plus a real-interaction pass (including a real click confirming the just-shipped theme-toggle fix on the live site); reconciled stale claims in this file, `README.md`, `PLAN.md`, and `CLAUDE.md` (notably: `hi` was documented as a player name in four places — it's the numeric high score). Full receipt: `qa-reports/2026-09-02_GhostCode_CookoutP4_PublicTruthSync.md`. No push/merge/deploy.

## Next Actions (all optional)
- **Branch prune (Rory):** the pre-rebrand stale branches above, plus the newer local-only staging branches from the uplift/light-mode arc — all already landed on `main`.
- **Phantom one-eyed on phones (parked P-6):** cosmetic mobile rendering issue, not yet fixed — pick up during a future mobile-visual pass, not urgent.
- **`.claude/launch.json`'s `pacman-dev` config:** its `$PORT` placeholder isn't substituted by this session's preview tooling (`python3 -m http.server $PORT` fails with an invalid-port error) — worked around by running the server directly for this pass; worth a real fix so the saved launch config works again.
- **Phase 4 growth (Quinn):** spaced-repetition / per-category stats / new command categories — still just an idea, gated on Sky's appetite.
- **prompt-library link fix:** unrelated to Ghost Code, carried over from an old note — verify still relevant before acting.

## Open Risks
- None blocking.
- (Minor) Vercel/GitHub Pages rebuild lag after push, as before.
- `.state-snapshot.yaml`, `.features-brief.yaml`, and `PROJECT_DIGEST.yaml` in this repo look like stale/dead automation artifacts (one carries a last-commit date from 2024, before this project existed) — likely owned by an external dashboard tool; left untouched by this pass, out of scope.
